# Confirmed Unity / GDD decisions

This directory mirrors the design decisions that have been confirmed for the
Unity version of **SYMBIOSIS: 49**.

- `CONFIRMED_DESIGN_SPEC.md` is the living confirmed visual and interaction
  specification.
- `Images/` contains compressed WebP previews used by this GitHub document.
- Production-resolution PNG assets and Unity-import copies remain in the local
  Unity project; these WebP files are documentation previews only.
- The final section of the specification lists items that remain explicitly
  unconfirmed, so they cannot be mistaken for approved decisions.

Last synchronized: 2026-09-21.
